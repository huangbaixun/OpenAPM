# 啄木鸟 APM 2.0 · 设计讨论稿

> 这不是规格文档。原型本身就是规格 —— 直接打开点几下就懂了。
> 这份文档的目的是告诉你 **我做了哪些设计选择、为什么、以及哪些点想征求你的意见**。
> 看完预计 15 分钟，然后请用「原型 + `?` 注释」来对照具体页面讨论。

---

## 1. 这是什么

啄木鸟 APM 2.0 是面向 **多集群 / 多业务域** 场景的一站式可观测性平台原型，覆盖：

- **应用层**：Service / Endpoint / Trace / Log / Exception / Topology
- **基础设施层**：Database / Redis / Kafka（按 cluster 聚合）
- **业务层**：Dashboards / SLO / Alerts / LLM 成本归因
- **平台层**：成员、采样、Token、审计

定位：替代「Grafana + Jaeger + Kibana + n9e + 自研脚本」的拼盘体验，给 SRE 和后端工程师一个**统一的故障定位起点**。

---

## 2. 5 分钟导览（建议按顺序点）

> 已部署：https://huangbaixun.github.io/OpenAPM/
> 在任何页面按 **`?`** 显示该页的设计注释；按 **`⌘K`** / `Ctrl+K` 打开命令面板。

| # | 路径 | 你应该注意什么 |
|---|---|---|
| 1 | Overview | 服务卡片 + 健康度排序；点 `payment-service` |
| 2 | Service · checkout-service | 6 个一级 tab；进 *Signals* 看 nested subtab；顶部三按钮（SLO / 告警 / Trace） |
| 3 | 按 `⌘K`，搜索 `mysql-prod-master` | 命令面板 fuzzy 匹配 → 跳到 DB 集群详情 |
| 4 | DB 集群详情 → 「跳到 Trace」 | 验证「集群 → 调用链」的跨页参数透传 |
| 5 | Traces 页面 | 条件构造器 + Waterfall + Logs / Service Map / JSON 四视图 |
| 6 | Logs 页面 | 点任一行的 `trace=...` 链接 → 自动跳到对应 trace |
| 7 | Topology | 服务依赖图 + 时间区间过滤（拖动可见数字变化） |
| 8 | Alerts → 任一告警「查看 trace」 | 验证「告警 → 调用链」反向贯通 |

---

## 3. 关键设计选择 · 想要的反馈

每条结构：**选择 / 为什么 / 考虑过的替代 / 想征求意见的点**。

### 3.1 多集群是一等公民
- **选择**：DB / Redis / Kafka 顶层都先选「集群」（cluster）再看资源。集群是过滤维度，不是装饰。
- **为什么**：实际故障定位中 90% 是「某集群」级问题（主从切换、网络分区、资源饱和），而不是「某条 SQL」。先聚类再下钻。
- **替代**：扁平展示（Grafana 风格），靠 dropdown 过滤。试过感觉信息密度过高，新手不知道从哪入手。
- **想征求意见**：
  - [ ] 顶层是否需要「全局集群过滤器」，影响所有 infra 页面？还是各自维护？
  - [ ] 集群和「环境（prod/staging/dev）」是不是同一维度？现在分开了。

### 3.2 服务详情：10 tab → 6 tab + nested subtab
- **选择**：服务详情顶层 6 个 tab：`Overview / Endpoints / Signals / Dependencies / Runtime / Exceptions`。Signals 里再嵌 Traces+Logs 两个子 tab；Dependencies 嵌 DB+Kafka+External。
- **为什么**：原本 10 个一级 tab 横向溢出，且 Traces/Logs 用户很少独立看，几乎都是一起看。
- **替代**：左侧二级导航树。试过太重，单服务不需要那么深。
- **想征求意见**：
  - [ ] Signals 把 Traces 和 Logs 合并是否合理？还是留独立 tab？
  - [ ] Runtime 子 tab（process/instances/jvm）切分粒度合不合适？
  - [ ] Exceptions 是否应该并入 Signals？

### 3.3 ⌘K 命令面板
- **选择**：单一入口跳转：服务 / 集群 / 页面 / 环境 / 项目 / 操作。fuzzy 匹配，分组展示，49 个候选。
- **为什么**：传统侧边栏导航做不了「我要找 mysql-prod-master 这个集群」级别的精度。
- **替代**：每页加自己的搜索框。不一致、学习成本高。
- **想征求意见**：
  - [ ] 是否值得花精力把所有跳转都收口到 ⌘K？还是侧边栏 + ⌘K 双轨？
  - [ ] 候选条目 49 个会不会变成 500 个时性能顶不住？要不要做服务端搜索？

### 3.4 路由参数单次消费模式
- **选择**：`APM.go('traces', { svcFilter: 'checkout' })` 透传参数，目标页面 render 时 read + delete。下次刷新或返回不会带回旧参数。
- **为什么**：避免「我从 A 跳过来设了过滤，再从 B 跳过来还残留 A 的过滤」这种 ghost state。
- **替代**：参数写入 URL hash 持久化。但很多筛选是临时的，写 URL 反而污染。
- **想征求意见**：
  - [ ] 哪些过滤值得写 URL（可分享）？哪些应该单次消费？现在的边界划得对吗？

### 3.5 健康度颜色 vs 数值
- **选择**：服务卡片顶部用色环（绿/黄/红）+ 健康度数字（0-100）。色环主导视觉，数字辅助。
- **为什么**：扫视时只看颜色就够；定位时再看数字。
- **替代**：纯数字 / 纯告警计数。试过都不如色环直观。
- **想征求意见**：
  - [ ] 健康度 = `f(error_rate, p99, apdex, alerts)` 的算法是否需要可配置？
  - [ ] 红/黄阈值（健康度 < 50 / < 80）合不合理？

### 3.6 双向贯通：告警 ↔ Trace ↔ Log ↔ Exception
- **选择**：每个告警/异常/日志行都有「查看 trace」入口；trace 详情里有 Logs tab 反向定位日志。
- **为什么**：故障定位不是单向的「先告警再 trace」，可能从 grep 日志逆推 trace。
- **想征求意见**：
  - [ ] 是否需要「按 trace_id 反查所有出现位置」的全文索引视图？
  - [ ] Exception 详情堆栈里的每一帧能不能跳到 GitLab 源码？需要源码映射服务。

### 3.7 Topology：时间区间重放
- **选择**：拓扑图支持时间窗口（15m/1h/6h/24h），切换时节点 RPM/错误率数字按系数重放。
- **为什么**：拓扑不是静态视图，是「故障当时的样子」。事后复盘价值高。
- **当前未做**：拖动时间轴的 scrubber（P2 待办）。
- **想征求意见**：
  - [ ] scrubber 拖动是逐秒回放还是按 5 分钟桶？数据量级影响很大。
  - [ ] 是否要做「事故标记点」叠加（红色竖线代表告警时刻）？

### 3.8 Settings：CRUD 全用模态弹窗
- **选择**：成员邀请 / 采样规则 / Token 生成 / 数据保留策略 全部弹窗，不进 routed sub-page。
- **为什么**：这些操作都是「一次性写入」，没有持久化的中间状态需要 URL 化。
- **替代**：每个 CRUD 走独立路由。试过太重，绝大多数管理员一周用一次。
- **想征求意见**：
  - [ ] Token 生成后只显示一次的复制流程，是否需要「下载为文件」选项？
  - [ ] 审计日志是否应该独立成顶层页面（不放 Settings）？

---

## 4. 全局开放问题

这些不锚定到具体页面，但需要团队对齐：

1. **多租户模型**：当前 Domain → Project → Service 三级。是否需要 Team / Squad 级聚合？
2. **权限粒度**：现在按角色（Owner/Admin/Member/Viewer）。是否需要按 Project / Service 级 ACL？
3. **告警接收方**：原型只显示告警，没有「订阅 / 通知通道」配置入口。要做的话放哪？
4. **数据保留 vs 成本**：保留策略只在 Settings 里有一个全局开关。是否要按 Project 配？
5. **移动端**：当前固定 1440px。要不要做响应式？目标用户多少在手机上看？
6. **国际化**：当前中英双语硬编码。要不要走 i18n key？

---

## 5. 怎么反馈

- **页面级别**：在原型上按 `?` 看注释 → 直接在该页评论里贴截图
- **设计原则级别**：在本文档第 3 节对应小节下评论
- **想新加的功能**：本文档第 4 节末尾追加，不要直接改原型代码

集中讨论一次后，我会汇总成 V2.1 的设计变更清单。

---

## 6. 原型实现说明（dev 关心的）

> 这一节是给真要落地的工程师看的，不影响讨论。

- **栈**：纯 HTML + 原生 JS + CSS 自定义属性。无构建系统、无框架、无包管理器。
- **入口**：`啄木鸟 APM 2.0.html`（中文文件名）+ `index.html`（GitHub Pages 根目录别名）。
- **架构**：全局 `APM` 对象 + 渲染函数返回 HTML 字符串。详见 `CLAUDE.md`。
- **Mock 数据**：`js/data.js` 全部硬编码。所有数字都是 fixture，不要把它当真实数据看。
- **设计 token**：`styles/core.css` 里的 CSS custom properties 是单一来源；落地时直接拷过去。
- **UI 库借鉴**：Apple HIG 风格 + Linear/Notion 的间距感。**不要**直接用 antd / shadcn，留白和阴影规则不一样。

落地路径建议：
1. 先抽 design token（颜色 / 间距 / 字号 / 圆角）→ 落地为团队 design system
2. 再按页面优先级实施：Overview / Service / Traces / Logs 是 P0
3. Topology / Dashboards 留 P1（这两块工作量最大）
4. Settings / Onboarding 最后做（功能简单但需要联通后端）
